diff a/server/controllers/adminAnalyticsController.js b/server/controllers/adminAnalyticsController.js	(rejected hunks)
@@ -1,4 +1,5 @@
 const Booking = require("../models/Booking")
+const Invoice = require("../models/Invoice")
 
 exports.getAnalytics = async (
   req,
@@ -22,40 +23,34 @@
         },
       })
 
+    // Popular service is based on what was actually billed on
+    // published invoices, not just what customers originally
+    // requested when booking (a booking can end up billed for a
+    // different service than what was first requested).
     const serviceStats =
-      await Booking.aggregate([
+      await Invoice.aggregate([
+        { $match: { isPublished: true } },
+        { $unwind: "$services" },
         {
           $group: {
-            _id: "$service",
-            count: {
-              $sum: 1,
-            },
+            _id: "$services",
+            count: { $sum: 1 },
           },
         },
+        { $sort: { count: -1 } },
       ])
 
-    let revenue = 0
+    // Revenue comes from actually-published invoices — not a flat
+    // guess per completed booking.
+    const revenueResult = await Invoice.aggregate([
+      { $match: { isPublished: true } },
+      { $group: { _id: null, total: { $sum: "$totalAmount" } } },
+    ])
 
-    const completed =
-      await Booking.find({
-        status: "Completed",
-      })
-
-    completed.forEach(
-      (booking) => {
-
-        revenue += Number(
-          booking.price || 2500
-        )
-
-      }
-    )
+    const revenue = revenueResult[0]?.total || 0
 
     const popularService =
-      serviceStats.sort(
-        (a, b) =>
-          b.count - a.count
-      )[0]
+      serviceStats[0]
 
     res.json({
 
