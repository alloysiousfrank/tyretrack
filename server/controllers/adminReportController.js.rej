diff a/server/controllers/adminReportController.js b/server/controllers/adminReportController.js	(rejected hunks)
@@ -1,43 +1,55 @@
 const Booking = require("../models/Booking")
 const User = require("../models/User")
+const Invoice = require("../models/Invoice")
 
 exports.getReports = async (req,res)=>{
 
-  const totalUsers =
-    await User.countDocuments()
+  try {
 
-  const totalBookings =
-    await Booking.countDocuments()
+    const totalUsers =
+      await User.countDocuments()
 
-  const completedBookings =
-    await Booking.countDocuments({
-      status:"Completed"
-    })
+    const totalBookings =
+      await Booking.countDocuments()
 
-  const pendingBookings =
-    await Booking.countDocuments({
-      status:{
-        $ne:"Completed"
-      }
+    const completedBookings =
+      await Booking.countDocuments({
+        status:"Completed"
+      })
+
+    const pendingBookings =
+      await Booking.countDocuments({
+        status:{
+          $ne:"Completed"
+        }
+      })
+
+    // Revenue comes from actually-published invoices — not a flat
+    // ₹2500-per-booking guess.
+    const revenueResult = await Invoice.aggregate([
+      { $match: { isPublished: true } },
+      { $group: { _id: null, total: { $sum: "$totalAmount" } } },
+    ])
+
+    const revenue = revenueResult[0]?.total || 0
+
+    res.json({
+      success: true,
+      totalUsers,
+      totalBookings,
+      completedBookings,
+      pendingBookings,
+      revenue
     })
 
-  const revenue =
-    completedBookings * 2500
+  } catch (error) {
 
-  res.json({
-    totalUsers,
-    totalBookings,
-    completedBookings,
-    pendingBookings,
-    revenue
-  })
+    console.log(error)
 
-}
-const {
- getReports
-}
-=
-require(
- "../controllers/adminReportController"
-)
+    res.status(500).json({
+      success: false,
+    })
+
+  }
 
+}
