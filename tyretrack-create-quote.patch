diff --git a/src/components/admin/AdminLayout.css b/src/components/admin/AdminLayout.css
index c281a38..515878b 100644
--- a/src/components/admin/AdminLayout.css
+++ b/src/components/admin/AdminLayout.css
@@ -44,10 +44,16 @@ CONTENT
 
     flex:1;
 
+    width:100%;
+
+    min-width:0;
+
     margin-left:0;
 
     padding:32px;
 
+    box-sizing:border-box;
+
     overflow-x:hidden;
 
     overflow-y:auto;
@@ -56,7 +62,41 @@ CONTENT
 
     min-height:100vh;
 
-    transition:.35s ease;
+    transition:margin-left .35s ease;
+
+}
+
+/* ===========================
+DESKTOP / LAPTOP (>=1024px)
+sidebar is persistent, no drawer,
+hamburger hidden, content shifts right
+=========================== */
+
+@media(min-width:1024px){
+
+.menu-btn{
+
+display:none;
+
+}
+
+.admin-content{
+
+margin-left:290px;
+
+padding:40px 44px;
+
+}
+
+}
+
+@media(min-width:1440px){
+
+.admin-content{
+
+padding:48px 60px;
+
+}
 
 }
 
diff --git a/src/components/admin/AdminLayout.tsx b/src/components/admin/AdminLayout.tsx
index bd145e0..9492cf9 100644
--- a/src/components/admin/AdminLayout.tsx
+++ b/src/components/admin/AdminLayout.tsx
@@ -2,6 +2,7 @@ import { Outlet, useLocation } from "react-router-dom"
 import { useState, useEffect, useRef } from "react"
 import AdminSidebar from "./AdminSidebar"
 import "./AdminLayout.css"
+import "../../styles/admin-responsive.css"
 
 export default function AdminLayout() {
 
diff --git a/src/components/admin/AdminSidebar.css b/src/components/admin/AdminSidebar.css
index 4403860..a147043 100644
--- a/src/components/admin/AdminSidebar.css
+++ b/src/components/admin/AdminSidebar.css
@@ -279,6 +279,47 @@ left:-270px;
 
 }
 
+/* ===========================
+DESKTOP / LAPTOP (>=1024px)
+sidebar stays open permanently,
+overlay is disabled entirely
+=========================== */
+
+@media(min-width:1024px){
+
+.admin-sidebar{
+
+left:0;
+
+box-shadow:none;
+
+}
+
+.sidebar-overlay{
+
+display:none;
+
+}
+
+}
+
+/* ===========================
+TABLET (768px - 1023px)
+drawer sidebar, slightly narrower
+=========================== */
+
+@media(min-width:769px) and (max-width:1023px){
+
+.admin-sidebar{
+
+width:280px;
+
+left:-280px;
+
+}
+
+}
+
 
 .admin-menu{
 
diff --git a/src/pages/Admin.tsx b/src/pages/Admin.tsx
index fcb6d6c..5e35538 100644
--- a/src/pages/Admin.tsx
+++ b/src/pages/Admin.tsx
@@ -307,9 +307,6 @@ window.location.href =
 
 <div
  className="admin-page"
- style={{
-  marginLeft: "270px"
- }}
 >
 
       <div className="admin-container">
diff --git a/src/pages/AdminLogin.css b/src/pages/AdminLogin.css
index 770b6b7..8d96ae1 100644
--- a/src/pages/AdminLogin.css
+++ b/src/pages/AdminLogin.css
@@ -66,4 +66,35 @@
 .admin-login-card button:hover {
   background: #ff4444;
   transform: translateY(-2px);
+}
+
+@media (max-width: 600px) {
+  .admin-login-card {
+    padding: 36px 26px;
+    border-radius: 22px;
+  }
+
+  .admin-login-card h1 {
+    font-size: 2rem;
+    margin-bottom: 26px;
+  }
+}
+
+@media (max-width: 380px) {
+  .admin-login-page {
+    padding: 14px;
+  }
+
+  .admin-login-card {
+    padding: 28px 18px;
+  }
+
+  .admin-login-card h1 {
+    font-size: 1.7rem;
+  }
+
+  .admin-login-card input,
+  .admin-login-card button {
+    padding: 15px;
+  }
 }
\ No newline at end of file
diff --git a/src/pages/AdminQuotes.css b/src/pages/AdminQuotes.css
index f3941f2..db56c45 100644
--- a/src/pages/AdminQuotes.css
+++ b/src/pages/AdminQuotes.css
@@ -18,10 +18,60 @@ justify-content:space-between;
 
 align-items:center;
 
+gap:20px;
+
 margin-bottom:35px;
 
 }
 
+.create-quote-btn{
+
+flex-shrink:0;
+
+background:linear-gradient(135deg,#ff3131,#ff5f2d);
+
+border:none;
+
+padding:15px 26px;
+
+border-radius:14px;
+
+color:white;
+
+font-size:15px;
+
+font-weight:700;
+
+cursor:pointer;
+
+transition:.3s ease;
+
+box-shadow:0 12px 30px rgba(255,40,40,.25);
+
+}
+
+.create-quote-btn:hover{
+
+transform:translateY(-2px);
+
+box-shadow:0 16px 36px rgba(255,40,40,.4);
+
+}
+
+.admin-quote-create-overlay{
+
+position:fixed;
+
+inset:0;
+
+z-index:9999;
+
+overflow-y:auto;
+
+background:#090909;
+
+}
+
 .quotes-header h1{
 
 font-size:34px;
diff --git a/src/pages/AdminQuotes.tsx b/src/pages/AdminQuotes.tsx
index 4949917..fc06050 100644
--- a/src/pages/AdminQuotes.tsx
+++ b/src/pages/AdminQuotes.tsx
@@ -1,5 +1,6 @@
 import { useEffect, useState } from "react"
 import QuoteEditor from "../components/quotations/QuoteEditor"
+import GetQuote from "./GetQuote"
 import "./AdminQuotes.css"
 interface Quotation {
 
@@ -25,6 +26,7 @@ interface Quotation {
 
 export default function AdminQuotes() {
 const [selectedQuote,setSelectedQuote]=useState("")
+const [creatingQuote,setCreatingQuote]=useState(false)
   const [quotes, setQuotes] = useState<Quotation[]>([])
 
   useEffect(() => {
@@ -85,6 +87,20 @@ const [selectedQuote,setSelectedQuote]=useState("")
 
         </div>
 
+        <button
+
+            type="button"
+
+            className="create-quote-btn"
+
+            onClick={()=>setCreatingQuote(true)}
+
+        >
+
+            + Create Quotation
+
+        </button>
+
     </div>
 
     {/* STATS */}
@@ -310,6 +326,35 @@ Open
 </div>
 {
 
+creatingQuote && (
+
+<div className="admin-quote-create-overlay">
+
+<GetQuote
+
+embedded
+
+onClose={()=>setCreatingQuote(false)}
+
+onCreated={(id)=>{
+
+setCreatingQuote(false)
+
+setSelectedQuote(id)
+
+fetchQuotes()
+
+}}
+
+/>
+
+</div>
+
+)
+
+}
+{
+
 selectedQuote && (
 
 <QuoteEditor
diff --git a/src/pages/GetQuote.css b/src/pages/GetQuote.css
index 60711cd..c1f8c27 100644
--- a/src/pages/GetQuote.css
+++ b/src/pages/GetQuote.css
@@ -31,6 +31,58 @@ overflow-x:hidden;
 
 }
 
+/* ===========================
+ADMIN-EMBEDDED MODE
+Used when this wizard is opened
+from the AdminQuotes "Create
+Quotation" button instead of the
+public /getquote route.
+=========================== */
+
+.quote-page.embedded{
+
+    padding:80px 0 60px;
+
+}
+
+.quote-close-btn{
+
+    position:fixed;
+
+    top:24px;
+
+    right:24px;
+
+    width:48px;
+
+    height:48px;
+
+    border:none;
+
+    border-radius:14px;
+
+    background:rgba(255,255,255,.08);
+
+    color:#fff;
+
+    font-size:20px;
+
+    cursor:pointer;
+
+    z-index:10;
+
+    transition:.25s ease;
+
+}
+
+.quote-close-btn:hover{
+
+    background:#ff2b2b;
+
+    transform:translateY(-2px);
+
+}
+
 .quote-overlay{
 
     position:absolute;
diff --git a/src/pages/GetQuote.tsx b/src/pages/GetQuote.tsx
index 341e2d4..328cf4d 100644
--- a/src/pages/GetQuote.tsx
+++ b/src/pages/GetQuote.tsx
@@ -2,7 +2,17 @@ import { useState } from "react"
 import axios from "axios"
 import "./GetQuote.css"
 
-export default function GetQuote() {
+interface GetQuoteProps {
+  embedded?: boolean
+  onCreated?: (id: string) => void
+  onClose?: () => void
+}
+
+export default function GetQuote({
+  embedded = false,
+  onCreated,
+  onClose
+}: GetQuoteProps = {}) {
 const [customerName, setCustomerName] = useState("")
 const [phone, setPhone] = useState("")
 const [email, setEmail] = useState("")
@@ -91,6 +101,14 @@ notes
 
 if(response.data.success){
 
+if(onCreated){
+
+onCreated(response.data.quotation._id)
+
+return
+
+}
+
 alert(
 
 `Quotation Submitted Successfully!
@@ -140,10 +158,30 @@ error.response?.data?.message ||
 }
   return (
 
-    <section className="quote-page">
+    <section className={`quote-page ${embedded ? "embedded" : ""}`}>
 
       <div className="quote-overlay"></div>
 
+      {embedded && (
+
+        <button
+
+          type="button"
+
+          className="quote-close-btn"
+
+          onClick={onClose}
+
+          aria-label="Close"
+
+        >
+
+          ✕
+
+        </button>
+
+      )}
+
       <div className="quote-container">
 
         {/* HERO */}
@@ -152,20 +190,23 @@ error.response?.data?.message ||
 
           <p className="quote-tag">
 
-            TYRETRACK PREMIUM AUTO CARE
+            {embedded ? "TYRETRACK ADMIN PANEL" : "TYRETRACK PREMIUM AUTO CARE"}
 
           </p>
 
           <h1>
 
-            Premium Tyre Quotation
+            {embedded ? "Create New Quotation" : "Premium Tyre Quotation"}
 
           </h1>
 
           <p className="quote-description">
 
-            Get a personalised quotation for your vehicle
-            within minutes.
+            {embedded
+
+              ? "Enter the customer and vehicle details to start a new quotation."
+
+              : "Get a personalised quotation for your vehicle within minutes."}
 
           </p>
 
@@ -909,22 +950,26 @@ Customer Details
 
 <div>
 
-✅ Your quotation request will be reviewed by our TyreTrack experts.
+{embedded ? "✅ This will create a new quotation record." : "✅ Your quotation request will be reviewed by our TyreTrack experts."}
 
 </div>
 
 <div>
 
-📞 Our team may contact you if additional information is required.
+{embedded ? "💰 You'll be taken straight to pricing after this." : "📞 Our team may contact you if additional information is required."}
 
 </div>
 
+{!embedded && (
+
 <div>
 
 📄 A professional quotation will be generated and shared with you.
 
 </div>
 
+)}
+
 </div>
 
 <button
@@ -935,7 +980,7 @@ onClick={submitQuotation}
 
 >
 
-Submit Premium Quotation Request
+{embedded ? "Create Quotation" : "Submit Premium Quotation Request"}
 
 </button>
 
diff --git a/src/styles/admin-responsive.css b/src/styles/admin-responsive.css
new file mode 100644
index 0000000..b0f5a11
--- /dev/null
+++ b/src/styles/admin-responsive.css
@@ -0,0 +1,216 @@
+/* ==========================================================
+   TYRETRACK ADMIN — GLOBAL RESPONSIVE LAYER
+   Loaded once (via AdminLayout) for every admin page.
+   Breakpoints:
+     >=1024px  Desktop / Laptop  — persistent sidebar
+     769-1023  Tablet            — drawer sidebar, 2-col grids
+     481-768   Large mobile      — drawer sidebar, 1-col
+     <=480     Small mobile      — compact spacing/type
+   This file only tightens/guards existing component CSS —
+   it never overrides colors, gradients, or component logic.
+   ========================================================== */
+
+/* Prevent any admin element from ever forcing horizontal
+   scroll on the page itself; individual wide elements (tables)
+   get their own scroll container instead. */
+.admin-content,
+.admin-content * {
+  box-sizing: border-box;
+}
+
+.admin-content img,
+.admin-content svg {
+  max-width: 100%;
+  height: auto;
+}
+
+/* ==========================================================
+   TABLES — always horizontally scrollable on their own,
+   never squeeze columns or blow out the page width.
+   ========================================================== */
+
+.quotes-table {
+  overflow-x: auto;
+  overflow-y: hidden;
+  -webkit-overflow-scrolling: touch;
+}
+
+.quotes-table table {
+  min-width: 720px;
+}
+
+.invoice-preview {
+  overflow-x: auto;
+  -webkit-overflow-scrolling: touch;
+}
+
+.invoice-table {
+  min-width: 360px;
+}
+
+/* ==========================================================
+   DEFENSIVE FLEX WRAPPING
+   Rows built with justify-content:space-between should wrap
+   instead of crushing their contents on narrow viewports.
+   ========================================================== */
+
+.admin-header,
+.quotes-header,
+.invoice-header,
+.history-header,
+.history-banner,
+.history-highlight-card,
+.invoice-mode {
+  flex-wrap: wrap;
+  row-gap: 14px;
+}
+
+/* ==========================================================
+   TABLET (<=1023px)
+   ========================================================== */
+
+@media (max-width: 1023px) {
+  .quote-stats {
+    grid-template-columns: repeat(2, 1fr);
+  }
+
+  .profile-stats {
+    grid-template-columns: repeat(3, 1fr);
+    gap: 12px;
+  }
+}
+
+/* ==========================================================
+   LARGE MOBILE (<=768px)
+   ========================================================== */
+
+@media (max-width: 768px) {
+  .admin-page {
+    padding: 100px 16px 60px;
+  }
+
+  .admin-quotes {
+    padding: 20px 16px;
+  }
+
+  .admin-header,
+  .quotes-header {
+    flex-direction: column;
+    text-align: center;
+    justify-content: center;
+    gap: 14px;
+  }
+
+  .admin-header h1 {
+    width: 100%;
+  }
+
+  .admin-header .clear-btn,
+  .admin-header .logout-btn,
+  .quotes-header .create-quote-btn {
+    margin: 0;
+    width: 100%;
+  }
+
+  .admin-controls {
+    flex-direction: column;
+    align-items: stretch;
+  }
+
+  .admin-search,
+  .admin-filter,
+  .filter-select {
+    width: 100%;
+    min-width: 0;
+  }
+
+  .admin-stats {
+    grid-template-columns: repeat(2, 1fr);
+    gap: 14px;
+  }
+
+  .stat-card {
+    padding: 20px;
+  }
+
+  .stat-card p {
+    font-size: 1.7rem;
+  }
+
+  .admin-nav {
+    justify-content: center;
+  }
+
+  .admin-nav button {
+    flex: 1 1 140px;
+  }
+
+  .history-banner,
+  .history-highlight-card,
+  .profile-top {
+    text-align: left;
+  }
+
+  .invoice-preview {
+    padding: 22px;
+  }
+
+  .invoice-header {
+    justify-content: center;
+    text-align: center;
+  }
+}
+
+/* ==========================================================
+   SMALL MOBILE (<=480px)
+   ========================================================== */
+
+@media (max-width: 480px) {
+  .admin-container h1,
+  .admin-page h1 {
+    font-size: 1.7rem;
+  }
+
+  .quotes-header h1 {
+    font-size: 1.5rem;
+  }
+
+  .admin-card,
+  .stat-card,
+  .quote-stat-card {
+    padding: 18px;
+  }
+
+  .admin-login-card {
+    padding: 30px 24px;
+  }
+
+  .admin-logo {
+    padding: 60px 18px 24px;
+  }
+
+  .profile-stats {
+    grid-template-columns: repeat(3, 1fr);
+    font-size: 16px;
+    gap: 8px;
+  }
+
+  .invoice-preview {
+    padding: 16px;
+  }
+}
+
+/* ==========================================================
+   VERY SMALL MOBILE (<=360px)
+   ========================================================== */
+
+@media (max-width: 360px) {
+  .admin-container h1,
+  .admin-page h1 {
+    font-size: 1.45rem;
+  }
+
+  .admin-nav button {
+    flex: 1 1 100%;
+  }
+}
